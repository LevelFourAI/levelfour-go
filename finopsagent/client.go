// Code generated by Fern. DO NOT EDIT.

package finopsagent

import (
	context "context"
	levelfour "github.com/LevelFourAI/levelfour-go"
	core "github.com/LevelFourAI/levelfour-go/core"
	internal "github.com/LevelFourAI/levelfour-go/internal"
	option "github.com/LevelFourAI/levelfour-go/option"
)

type Client struct {
	WithRawResponse *RawClient

	options *core.RequestOptions
	baseURL string
	caller  *internal.Caller
}

func NewClient(options *core.RequestOptions) *Client {
	return &Client{
		WithRawResponse: NewRawClient(options),
		options:         options,
		baseURL:         options.BaseURL,
		caller: internal.NewCaller(
			&internal.CallerParams{
				Client:      options.HTTPClient,
				MaxAttempts: options.MaxAttempts,
			},
		),
	}
}

// One turn, streamed as Vercel AI SDK data stream events.
//
// The adapter is built here rather than through `from_request`, because everything that can
// refuse a turn has to run before the stream opens: once the body is open the status is
// already sent, and a refusal after that reaches the user as prose rather than as an error.
//
// Letting FastAPI parse the body rather than reading it off the request also means a
// malformed one is a 422 from the framework instead of an exception inside the route.
func (c *Client) Chat(
	ctx context.Context,
	request *levelfour.ChatAPIV1AgentChatPostRequest,
	opts ...option.RequestOption,
) (any, error) {
	response, err := c.WithRawResponse.Chat(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) ListThreads(
	ctx context.Context,
	opts ...option.RequestOption,
) (*levelfour.ThreadListResponse, error) {
	response, err := c.WithRawResponse.ListThreads(
		ctx,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) GetThread(
	ctx context.Context,
	threadID string,
	opts ...option.RequestOption,
) (*levelfour.ThreadDetailResponse, error) {
	response, err := c.WithRawResponse.GetThread(
		ctx,
		threadID,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) DeleteThread(
	ctx context.Context,
	threadID string,
	opts ...option.RequestOption,
) (*levelfour.ThreadDeletedResponse, error) {
	response, err := c.WithRawResponse.DeleteThread(
		ctx,
		threadID,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Give a conversation a name of the person's choosing.
//
// A title the server derived from the opening question is a guess, and the sidebar is how
// somebody finds a conversation again. The rename outlives every later turn: `ensure`
// writes the title only on the arm that creates the thread.
func (c *Client) RenameThread(
	ctx context.Context,
	threadID string,
	request *levelfour.RenameThreadRequest,
	opts ...option.RequestOption,
) (*levelfour.ThreadRenamedResponse, error) {
	response, err := c.WithRawResponse.RenameThread(
		ctx,
		threadID,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Thumbs up or thumbs down on one answer.
//
// 201 on a re-vote as well as a first vote, following the reactions on a recommendation
// comment. The row is one per person per answer, so changing your mind replaces the vote
// rather than adding one, and the status describing the write rather than counting rows
// keeps the two indistinguishable to the caller, which is what they are.
func (c *Client) RateMessage(
	ctx context.Context,
	threadID string,
	messageID int,
	request *levelfour.MessageFeedbackRequest,
	opts ...option.RequestOption,
) (*levelfour.MessageFeedbackResponse, error) {
	response, err := c.WithRawResponse.RateMessage(
		ctx,
		threadID,
		messageID,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Un-press the thumb.
//
// Withdrawing a vote that was never cast is not an error: the caller wanted no vote on
// this answer and there is none. Only an id this conversation does not hold is a miss.
func (c *Client) ClearMessageFeedback(
	ctx context.Context,
	threadID string,
	messageID int,
	opts ...option.RequestOption,
) (*levelfour.MessageFeedbackResponse, error) {
	response, err := c.WithRawResponse.ClearMessageFeedback(
		ctx,
		threadID,
		messageID,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}
