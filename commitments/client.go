// Code generated by Fern. DO NOT EDIT.

package commitments

import (
	context "context"
	levelfour "github.com/LevelFourAI/levelfour-go"
	core "github.com/LevelFourAI/levelfour-go/core"
	internal "github.com/LevelFourAI/levelfour-go/internal"
	option "github.com/LevelFourAI/levelfour-go/option"
)

type Client struct {
	WithRawResponse *RawClient

	options *core.RequestOptions
	baseURL string
	caller  *internal.Caller
}

func NewClient(options *core.RequestOptions) *Client {
	return &Client{
		WithRawResponse: NewRawClient(options),
		options:         options,
		baseURL:         options.BaseURL,
		caller: internal.NewCaller(
			&internal.CallerParams{
				Client:      options.HTTPClient,
				MaxAttempts: options.MaxAttempts,
			},
		),
	}
}

// ESR strip and aggregate RI/SP metrics for the requested provider (AWS real, GCP/Azure return zeros).
func (c *Client) GetOverview(
	ctx context.Context,
	request *levelfour.GetOverviewAPIV1CommitmentsOverviewGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentsOverviewResponse, error) {
	response, err := c.WithRawResponse.GetOverview(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Coverage per service and the covered spend per consuming account, with the on-demand remainder beside it so the percentage names its own denominator. Weighted by the dollars behind each figure, never averaged across commitments, where a hundred dollar reservation would move the number as far as a fifty thousand dollar one. A provider whose sweep has not run reports measured false rather than zero coverage, because holding none and not having looked are opposite answers.
func (c *Client) GetCoverageRates(
	ctx context.Context,
	request *levelfour.GetCoverageRatesAPIV1CommitmentsCoverageRatesGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CoverageRatesResponse, error) {
	response, err := c.WithRawResponse.GetCoverageRates(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Deprecated, use /coverage-rates. This reads one row per commitment held, so usage no commitment covers has no row and cannot appear, which is the larger half of coverage. on_demand_cost_monthly, saved_vs_od_monthly and filtered_savings_monthly are not measured here and are sent as zero, and filtered_coverage_pct averages percentages across commitments rather than weighting them by spend.
func (c *Client) GetCoverage(
	ctx context.Context,
	request *levelfour.GetCoverageAPIV1CommitmentsCoverageGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentsCoverageResponse, error) {
	response, err := c.WithRawResponse.GetCoverage(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) GetCoverageFilterOptions(
	ctx context.Context,
	request *levelfour.GetCoverageFilterOptionsAPIV1CommitmentsCoverageFilterOptionsGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CoverageFilterOptionsResponse, error) {
	response, err := c.WithRawResponse.GetCoverageFilterOptions(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Every reservation or plan the tenant holds, one row each, with the state the cloud reports rather than one derived from the end date. A plan that starts next month is queued and one whose payment failed is neither running nor expired, and no date says either. Attributes only some services carry arrive as null rather than zero.
func (c *Client) GetInventory(
	ctx context.Context,
	request *levelfour.GetInventoryAPIV1CommitmentsInventoryGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentInventoryResponse, error) {
	response, err := c.WithRawResponse.GetInventory(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// One entry per service for reservations, or per plan type for plans, each measured on its own and never rolled up: EC2 and RDS contract in vCPU while Redshift, ElastiCache, OpenSearch, Elasticsearch and MemoryDB contract in nodes, so an average across them describes neither. Each entry's headline divides the used hours by the purchased hours over the window rather than averaging its dimensions.
func (c *Client) GetUtilization(
	ctx context.Context,
	request *levelfour.GetUtilizationAPIV1CommitmentsUtilizationGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentUtilizationResponse, error) {
	response, err := c.WithRawResponse.GetUtilization(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) ListCommitments(
	ctx context.Context,
	request *levelfour.ListCommitmentsAPIV1CommitmentsGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentListResponse, error) {
	response, err := c.WithRawResponse.ListCommitments(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Priced from the current offering rather than from the expiring term, whose price is as old as the term. error_message carries a lookup that failed, so an unpriced renewal reads as unpriced rather than as zero.
func (c *Client) GetRenewalQuote(
	ctx context.Context,
	commitmentID string,
	opts ...option.RequestOption,
) (*levelfour.RenewalQuoteResponse, error) {
	response, err := c.WithRawResponse.GetRenewalQuote(
		ctx,
		commitmentID,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// The contract priced again as it stands, beside every other term its shape is sold on today, each priced at up to about forty quantities spanning what was sized and what is held, and recommended on exactly one row, above the quantity held only on a family basis, or on none when decision is let_expire: the commitment was used too little to pay for itself, so every row is priced at the quantity held for comparison and nothing is advised. With quantity, each offering is priced at exactly that quantity instead, one row each; a reservation is sold in whole units, so a fractional quantity is a 422. Without a stored series every quantity is still priced, and idle_units, spill_units and spill_cost come back null. sizing_basis says where the recommended quantity came from: the commitment's own fee line, which can only argue for buying less; family, that line plus the on-demand usage of its family and engine held on nine days in ten, the one basis that can argue for more; or unmeasured, where the quantity held stands; measured appears only on options an earlier sweep priced. upfront is the total paid at purchase for the row's quantity; recurring_hourly is the rate per unit. demand is the daily series the sizing read, with its projection across the longest term. A size-flexible RDS reservation can be renewed as a larger class of its family: resource is the class bought, every quantity and demand are counted in it, held_units_equivalent is a row's quantity in the class held, and quota says how many reservations the account may hold against what it would hold once the expiring ones are renewed; quota_blocked marks a renewal it could not hold in any class. size_mix is what the family runs by instance size and deployment.
func (c *Client) GetRenewalOptions(
	ctx context.Context,
	commitmentID string,
	request *levelfour.GetRenewalOptionsAPIV1CommitmentsRenewalOptionsCommitmentIDGetRequest,
	opts ...option.RequestOption,
) (*levelfour.RenewalOptionsResponse, error) {
	response, err := c.WithRawResponse.GetRenewalOptions(
		ctx,
		commitmentID,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) GetRenewal(
	ctx context.Context,
	commitmentID string,
	opts ...option.RequestOption,
) (*levelfour.CommitmentRenewalResponse, error) {
	response, err := c.WithRawResponse.GetRenewal(
		ctx,
		commitmentID,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Returns the recommendation that buys the new term, and the offering and quantity it is bound to. Calling it again never raises a rival: with no choice it returns the first one, and with a different choice it points a renewal nobody has decided on at that choice (rebound). rebindable says whether what it buys can still change: once anybody accepts, rejects or requests it, only the choice it was decided on is accepted. Nothing is bought here: the recommendation carries the offering, the quantity and the instant after which buying is safe, and the apply path acts on it.
func (c *Client) PostRenewal(
	ctx context.Context,
	commitmentID string,
	request *levelfour.PostRenewalAPIV1CommitmentsRenewalCommitmentIDPostRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentRenewalResponse, error) {
	response, err := c.WithRawResponse.PostRenewal(
		ctx,
		commitmentID,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) GetDetail(
	ctx context.Context,
	commitmentID string,
	opts ...option.RequestOption,
) (*levelfour.CommitmentDetailResponse, error) {
	response, err := c.WithRawResponse.GetDetail(
		ctx,
		commitmentID,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Returns a service x instrument matrix of RI and SP utilization, coverage, unused, and uncovered values. One row per service that has at least one commitment.
func (c *Client) GetByService(
	ctx context.Context,
	request *levelfour.GetByServiceAPIV1CommitmentsByServiceGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentsByServiceResponse, error) {
	response, err := c.WithRawResponse.GetByService(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) GetRecommendations(
	ctx context.Context,
	request *levelfour.GetRecommendationsAPIV1CommitmentsRecommendationsGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentRecommendationsResponse, error) {
	response, err := c.WithRawResponse.GetRecommendations(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// One row per commitment across every connected account, resolving the account that holds it against the accounts that consume it, with the expiry instant, the utilization trend, and what a renewal protects kept separate from what right-sizing would save.
func (c *Client) GetPortfolio(
	ctx context.Context,
	request *levelfour.GetPortfolioAPIV1CommitmentsPortfolioGetRequest,
	opts ...option.RequestOption,
) (*levelfour.CommitmentPortfolioResponse, error) {
	response, err := c.WithRawResponse.GetPortfolio(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// What the same usage would have cost at public on-demand rates against what it actually cost, for the organization and for each account that consumed the benefit. The rate is split into the part a negotiated agreement earns and the part commitments earn, because the public on-demand price sits above both and an account owning no commitment at all would otherwise report a healthy rate.
func (c *Client) GetEsr(
	ctx context.Context,
	request *levelfour.GetEsrAPIV1CommitmentsEsrGetRequest,
	opts ...option.RequestOption,
) (*levelfour.EsrResponse, error) {
	response, err := c.WithRawResponse.GetEsr(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// On-demand spend no commitment covers, split by platform and volatility. Carries the hourly floor as well as the average, because sizing against the average of a volatile base commits more than the base can sustain.
func (c *Client) GetUncovered(
	ctx context.Context,
	opts ...option.RequestOption,
) (*levelfour.UncoveredResponse, error) {
	response, err := c.WithRawResponse.GetUncovered(
		ctx,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// What to repurchase and when. units_recommended is the quantity the renewal options recommend, the same number the drawer shows: 0 with decision let_expire when the commitment was used too little to pay for itself, the quantity held when nothing measured it. units_consumed is the average the sized series measured, null when unmeasured. States the instant to buy after rather than a date, because a replacement bills from the moment it is bought, and lists the pending changes that must land first so their capacity is not reserved for another term.
func (c *Client) GetRenewalPlan(
	ctx context.Context,
	commitmentID string,
	opts ...option.RequestOption,
) (*levelfour.RenewalPlanResponse, error) {
	response, err := c.WithRawResponse.GetRenewalPlan(
		ctx,
		commitmentID,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Commitment relationships that behave like a commitment on the bill but appear in no provider commitment view, with the contracted floor separated from the metered leg that bills on top.
func (c *Client) GetContracts(
	ctx context.Context,
	opts ...option.RequestOption,
) (*levelfour.ContractsResponse, error) {
	response, err := c.WithRawResponse.GetContracts(
		ctx,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// Sets the organization's own tag on a commitment, or clears it with null. This is not the provider tags map on the inventory row, which is read from the cloud and never written here.
func (c *Client) UpdateTag(
	ctx context.Context,
	commitmentID string,
	request *levelfour.UpdateCommitmentTagRequest,
	opts ...option.RequestOption,
) (*levelfour.UpdateCommitmentTagResponse, error) {
	response, err := c.WithRawResponse.UpdateTag(
		ctx,
		commitmentID,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}
